# コピー時に以下 2 点を置換すること（本ファイルはあくまで雛形）:
#   1. ファイル名のタイムスタンプを `rails generate migration` の規約に揃える
#      例: cp 00000000000000_add_tokens_valid_after_to_users.rb.template \
#          db/migrate/$(date +%Y%m%d%H%M%S)_add_tokens_valid_after_to_users.rb
#   2. ActiveRecord::Migration[<RAILS_MAJOR.MINOR>] の <RAILS_MAJOR.MINOR> を
#      使用中の Rails のメジャー.マイナーに置換する（バージョンは固定しない方針：
#      ai-delivery/docs/environment-setup.md / tech-version-check スキル）
#
# 用途: 2 段階の失効（hard / soft）で hard 失効を DB 参照だけで判定するためのカラム。
class AddTokensValidAfterToUsers < ActiveRecord::Migration[<RAILS_MAJOR.MINOR>]
  def change
    add_column :users, :tokens_valid_after, :datetime
  end
end
