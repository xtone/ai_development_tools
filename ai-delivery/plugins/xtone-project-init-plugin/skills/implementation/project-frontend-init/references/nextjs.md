# references: nextjs（フロントエンド雛形レシピ）

> ⬜ **未実装スタブ**。本実装は `/aid-references-new project-frontend-init nextjs` で起こす（DP-AID-04）。契約は `../SKILL.md` を参照。

## 起こすべき内容（実装時に埋める）

- `create-next-app` 相当のワークスペース内雛形（App Router 前提・バージョンは tech-version-check で解決）
- ルート共有設定の継承（eslint / prettier / tsconfig base）
- `.env.local.sample`（実値は置かない）
- ビルド / 起動 / lint スクリプトのモノレポ連携

## templates

`../templates/nextjs/`（未作成）。
