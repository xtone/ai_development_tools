---
description: 要件収集。requirements-analyst を起動して requirements.schema.json を生成する。
argument-hint: [要件の説明テキスト]
---

requirements-analyst サブエージェントを使って、次のクライアント要件から `schemas/v1/requirements.schema.json` を作成してください。

要件説明: $ARGUMENTS

未決の判断ポイントは勝手に決めず、`undecided` に `DP-XXX` として記録し、`docs/pending-decisions.md` に起票してください（warn_and_document・ブロックなし）。
