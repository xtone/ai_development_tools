---
name: {{phase}}-{{skill_name}}
description: "<何をするか: 動詞+目的語> + <いつ使うか: トリガー> + <どんな条件で: ドメイン/規模/スタック>。例: Firebase Auth を使った認証設計をガイド。スタートアップ・BtoC アプリで MFA 規則・セッション設計・dAccount 連携を含む設計が必要なときに使う。"
---

# {{Skill Title}}

> SKL-12: `description` は Claude が Skill を選ぶための主要判断材料。3要素（何を / いつ / どんな条件で）を必ず含める。万能記述（「何でも処理できます」）は避ける。

## 概要

このスキルが担う作業を1〜2文で説明する。

## 入出力（スキーマ）

- 入力: `schemas/v1/<input>.schema.json`
- 出力: `schemas/v1/<output>.schema.json`

スキーマは編集しない（`schemas/` は xtone-shared-plugin への symlink, CONV-14）。

## 手順

1. …
2. …

## 判断ポイント（人間判断をスルーさせない）

人間判断が必要な箇所は AI が勝手に決めず、推奨だけ提示する。未決は `docs/pending-decisions.md` に記録し、判断ポイントカタログDB に DP-XXX として起票する（T-002 warn_and_document）。
